import React from 'react';
import { View } from 'react-native';

import { Container, Option, Title, Image} from './styles';

export default function Tips(){
  return(
    <Container>
      <Option>
        <Title>Pague as suas contas sem sair de casa</Title>
        <Image />
      </Option>
    </Container>
  )
}